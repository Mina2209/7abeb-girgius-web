# API base URL (no trailing slash)
VITE_API_BASE_URL=http://localhost:8080

# Media server URL for images/audio (used by getImageUrl.ts)
# In production, set to the actual media server origin (use HTTPS if behind a proxy)
VITE_MEDIA_SERVER_URL=http://34.231.244.96

# First-party analytics client gate
VITE_ANALYTICS_ENABLED=false
